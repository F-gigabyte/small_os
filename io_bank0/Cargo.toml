[package]
name = "io_bank0"
# From https://os.phil-opp.com/testing/ (accessed 6/02/2026)
test = true # Enable testing
version = "0.1.0"
edition = "2024"

[dependencies]
safe-mmio = "0.3.0"
small_os_lib = { path = "../small_os_lib" }
